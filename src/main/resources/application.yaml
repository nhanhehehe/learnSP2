server:
  port: 8080
  servlet:
    context-path: /identity

spring:
  application:
    name: nhan
  datasource:
    url: "jdbc:mysql://localhost:3306/user_system"
    username: root
    password: my-secret-pw
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
